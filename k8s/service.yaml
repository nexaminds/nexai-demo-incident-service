apiVersion: v1
kind: Service
metadata:
  name: nexai-demo-incident-service
  namespace: nexai-demo
  labels:
    app.kubernetes.io/name: nexai-demo-incident-service
    app.kubernetes.io/part-of: nexai-demo
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: nexai-demo-incident-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
